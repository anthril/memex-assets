# Log

Chronological ledger. Append-only. Auto-maintained by Memex. Entries use the prefix `## [YYYY-MM-DD] event | subject` for `grep`-ability (`grep "^## \[" log.md | tail -5`).

## [2026-04-23] init | research-wiki profile scaffolded

Wiki initialised from the `research-wiki` profile by `/memex:init`.
