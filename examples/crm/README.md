# CRM

Reshapes the workspace into a lightweight CRM. Adds frontmatter schemas
for `contact`, `company`, and `deal`, and ships filters for *Hot Leads*,
*Pipeline*, and *Recent Touchpoints*. Compatible with the `company` and
`engineering-ops` profiles — both are likely to track external
relationships in their workspace.

![CRM screenshot placeholder](./screenshot.png)
